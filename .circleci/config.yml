 version: 2
 jobs:
   one:
     docker:
       - image: circleci/ruby:2.4.1
     steps:
       - checkout
       - run: echo "running tests"
       - run: npm install
       - run: npm run test
   two:
     docker:
       - image: circleci/ruby:2.4.1
     steps:
       - checkout
       - run: echo "starting dev server"
       - run: npm install
       - run: npm start
 workflows:
   version: 2
   one_and_two:
     jobs:
       - one
       - two